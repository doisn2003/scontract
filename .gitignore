node_modules/
.env
backend/.env
frontend/node_modules/
backend/node_modules/
dist/
build/
.DS_Store

# Blockchain Artifacts
# We only want to keep the ABIs for the app to function in production.
# But build logs and debug files are too heavy for Git.
artifacts/
cache/